# Hello World

每个程序员的第一步就是输出 "Hello World"。在 Python 中，我们使用 `print()` 函数来输出内容。

## 语法

```python
print("Hello, World!")
```

## 说明

- `print()` 是 Python 的内置函数
- 字符串可以用双引号 `"` 或单引号 `'` 包围
- 括号 `()` 不可省略

## 动手试试

点击代码块上方的「Run」按钮执行代码，看看会发生什么！
