# KWDB Playground 架构问题汇总

> 本文档记录代码库中发现的架构和代码质量问题，按严重程度分类。
> 生成日期: 2026-02-10

---

## 🔴 严重问题 (Critical)

### 1. 容器ID解析逻辑缺陷 ✅ 已修复

**状态**: **已修复** (2026-02-10)

**位置**: `internal/docker/controller.go:189`

**问题描述**:
容器名称格式为 `kwdb-playground-{courseID}-{timestamp}`，但解析逻辑使用简单的字符串分割：
```go
courseID := strings.Join(parts[2:len(parts)-1], "-")
```
当课程ID本身包含连字符（如 `quick-start`）时，解析逻辑会错误地将时间戳的一部分也包含进课程ID。

**潜在影响**:
- 容器状态查询错误
- 课程切换时无法正确停止/启动容器
- 并发操作可能误操作其他课程的容器

**修复方案**:
1. **创建容器时添加 Labels 存储元数据** (`internal/docker/controller.go:913-919`):
```go
containerConfig := &container.Config{
    Labels: map[string]string{
        LabelAppName:   "kwdb-playground",
        LabelCourseID:  courseID,
        LabelVersion:   "1.0",
        LabelCreatedAt: time.Now().Format(time.RFC3339),
    },
}
```

2. **优先从 Labels 读取课程ID** (`internal/docker/controller.go:195-221`):
   - 首先尝试从 Docker Labels 读取 `kwdb-playground.course-id`
   - 如果 Labels 不存在，回退到名称解析（向后兼容旧容器）
   - 改进了名称解析逻辑，正确识别时间戳后缀

3. **更新清理逻辑** (`CleanupCourseContainers` 和 `CleanupAllContainers`):
   - 使用 Labels 匹配课程容器
   - 支持通过 Label `kwdb-playground.app` 识别 Playground 容器

**测试覆盖**:
- 添加了单元测试 `TestParseCourseIDFromContainerName`
- 测试覆盖标准课程ID、含连字符的课程ID、多个连字符的情况
- 验证向后兼容性

**相关文件**:
- `internal/docker/types.go`: 添加标签常量定义
- `internal/docker/controller.go`: 修改容器创建、加载、清理逻辑
- `internal/docker/controller_test.go`: 添加单元测试

---

### 2. SQL驱动单例并发问题

**位置**: `internal/api/routes.go:71`

**问题描述**:
```go
sqlDriver: &sql.Driver{},
```
`sqlDriver` 作为全局单例被所有课程共享，但内部维护单一连接池。当多个课程同时运行时，SQL查询会竞争同一连接池，可能导致：
- 查询结果被错误地路由到错误的数据库
- 连接池耗尽导致请求失败
- 事务隔离性问题

**潜在影响**:
- 数据泄露风险（A课程的查询可能返回B课程的数据）
- 并发性能瓶颈
- 数据库连接错误

**建议修复**:
为每个课程维护独立的连接池：
```go
type SQLDriverManager struct {
    drivers map[string]*sql.Driver // key: courseID
    mu      sync.RWMutex
}

func (m *SQLDriverManager) GetDriver(courseID string) *sql.Driver {
    // 按需创建和管理独立驱动实例
}
```

---

### 3. API层全局互斥锁性能瓶颈

**位置**: `internal/api/routes.go:45`

**问题描述**:
虽然 Docker 控制器内部已实现课程级别的互斥锁（`courseMu`），但 API 层 (`routes.go`) 仍使用全局的 `containerMutex`：
```go
h.containerMutex.Lock()
defer h.containerMutex.Unlock()
```
这导致任何课程的容器操作都会阻塞其他课程的操作，失去了课程级锁的意义。

**潜在影响**:
- 并发性能严重下降
- 用户体验差（启动A课程会阻塞B课程的停止操作）
- 无法充分利用多核CPU

**建议修复**:
移除API层的全局锁，完全依赖Docker控制器的课程级锁：
```go
// 删除 containerMutex 字段
// Docker 控制器内部的 getCourseMutex 已经提供了足够的并发控制
```

---

### 4. WebSocket 连接缺少心跳检测

**位置**: 
- `internal/api/routes.go:1173-1205`
- `internal/websocket/terminal.go`

**问题描述**:
WebSocket 终端连接没有实现 ping/pong 心跳机制。在网络不稳定、NAT超时、或客户端异常断开时，服务端无法及时感知连接已失效，导致：
- 僵尸会话长期占用资源
- 内存泄漏（goroutine 和连接句柄）
- 终端状态与实际连接状态不一致

**潜在影响**:
- 服务器资源耗尽
- 内存泄漏导致服务崩溃
- 用户看到"已连接"但实际会话已失效

**建议修复**:
```go
// 在 WebSocket 处理器中添加心跳
ticker := time.NewTicker(30 * time.Second)
defer ticker.Stop()

for {
    select {
    case <-ticker.C:
        if err := conn.WriteControl(websocket.PingMessage, []byte{}, time.Now().Add(10*time.Second)); err != nil {
            return // 连接已断开
        }
    // ... 其他逻辑
    }
}
```

---

## 🟡 中等问题 (Medium)

### 5. 前端状态管理混乱

**位置**: `src/pages/Learn.tsx`

**问题描述**:
Learn 页面使用了约20个独立的 `useState`，状态之间依赖关系复杂：
```typescript
const [course, setCourse] = useState<Course | null>(null)
const [currentStep, setCurrentStep] = useState(-1)
const [loading, setLoading] = useState(true)
const [containerId, setContainerId] = useState<string | null>(null)
const [containerStatus, setContainerStatus] = useState<ContainerStatus>('stopped')
// ... 还有15+个状态
```

状态逻辑分散在多个 `useEffect` 中，难以追踪和调试。

**潜在影响**:
- 状态更新逻辑复杂，容易引入bug
- 组件重渲染性能问题
- 难以测试和维护
- 状态不一致风险

**建议修复**:
使用 Zustand 重构状态管理：
```typescript
// src/store/learnStore.ts
import { create } from 'zustand'

interface LearnState {
  course: Course | null
  containerStatus: ContainerStatus
  currentStep: number
  // 使用 immer 简化嵌套状态更新
  actions: {
    startCourse: () => Promise<void>
    stopCourse: () => Promise<void>
    // ...
  }
}
```

---

### 6. 缺少前端API客户端封装

**位置**: 所有前端页面组件

**问题描述**:
前端直接使用 `fetch` 发起请求，缺少统一的封装：
- 错误处理不一致
- 重复的类型转换代码
- 没有请求拦截和重试机制
- 缺乏请求取消支持

**潜在影响**:
- 重复代码多
- 错误处理遗漏
- 用户体验不一致
- 竞态条件风险

**建议修复**:
创建统一的API客户端：
```typescript
// src/lib/api.ts
class ApiClient {
  async getCourses(): Promise<Course[]> {
    return this.request('/api/courses')
  }
  
  async startCourse(courseId: string, image?: string): Promise<StartCourseResponse> {
    return this.request(`/api/courses/${courseId}/start`, {
      method: 'POST',
      body: JSON.stringify({ image })
    })
  }
  
  private async request<T>(url: string, options?: RequestInit): Promise<T> {
    // 统一错误处理、重试、超时
  }
}
```

---

### 7. 配置验证不够严格

**位置**: `internal/config/config.go:113-136`

**问题描述**:
配置验证只记录警告而不阻止服务启动：
```go
if err := validateConfig(config, configLogger); err != nil {
    configLogger.Warn("Configuration validation warning: %v", err)
}
```

缺少：
- 端口冲突检查
- Docker socket 路径验证
- 课程目录可读性检查

**潜在影响**:
- 启动后才发现配置错误
- 生产环境服务异常
- 难以排查配置问题

**建议修复**:
将配置验证改为阻塞式，并提供明确的错误信息：
```go
func Load() (*Config, error) {
    config := &Config{...}
    if err := validateConfig(config); err != nil {
        return nil, fmt.Errorf("配置验证失败: %w", err)
    }
    return config, nil
}
```

---

### 8. 单元测试覆盖率不足

**位置**: 整个项目

**问题描述**:
项目中只有 E2E 测试（Playwright），缺乏：
- Go 单元测试（`*_test.go`）
- 前端组件测试
- Docker 控制器 mock 测试
- SQL 驱动测试

**潜在影响**:
- 回归 bug 难以发现
- 重构风险高
- 代码质量难以保证
- CI/CD 反馈慢

**建议修复**:
添加测试基础设施：
```go
// internal/docker/controller_test.go
func TestCreateContainer(t *testing.T) {
    mockClient := NewMockDockerClient()
    controller, _ := NewControllerWithClient(mockClient)
    
    // 测试各种场景
}
```

---

### 9. defer rows.Close() 在循环中使用

**位置**: `internal/api/routes.go:1435`

**问题描述**:
```go
defer rows.Close()
```
在循环中使用 `defer` 会导致资源延迟释放，直到函数返回时才关闭，可能导致连接池耗尽。

**潜在影响**:
- 数据库连接泄漏
- 连接池耗尽
- 性能下降

**建议修复**:
```go
for rows.Next() {
    // 处理行
}
rows.Close() // 显式关闭，不使用defer
```

---

## 🟢 轻微问题 (Low)

### 10. 代码重复

**位置**: `internal/api/routes.go`

**问题描述**:
`pauseCourse`, `resumeCourse`, `stopCourse` 等方法有几乎相同的容器查找逻辑：
```go
// 重复代码：查找容器的逻辑
containers, err := h.dockerController.ListContainers(ctx)
for _, container := range containers {
    if strings.HasPrefix(container.ID, coursePrefix) {
        // ...
    }
}
```

**建议修复**:
抽取为公共方法：
```go
func (h *Handler) findContainerByCourse(ctx context.Context, courseID string, states ...docker.ContainerState) (*docker.ContainerInfo, error) {
    // 统一的查找逻辑
}
```

---

### 11. 类型定义分散

**位置**: 多处

**问题描述**:
类型定义散落在多个文件中：
- `internal/docker/types.go`
- `internal/course/models.go`
- `src/types/container.ts`
- `src/types/port-conflict.ts`
- 组件内部的接口定义

缺少统一的类型管理策略。

**建议修复**:
- 后端：在每个包内定义自己的类型，通过接口隔离
- 前端：集中管理共享类型，组件专用类型定义在组件内

---

### 12. 日志格式不一致

**位置**: 整个后端代码

**问题描述**:
- 中英文日志混合
- 日志级别使用不一致
- 部分日志可能包含敏感信息

**建议修复**:
制定日志规范：
- 统一使用中文或英文（建议中文，符合现有代码风格）
- 使用结构化日志（JSON格式）
- 敏感信息脱敏处理

---

## 📊 问题统计

| 严重程度 | 数量 | 问题编号 |
|---------|------|---------|
| 🔴 严重 | 4 | 1, 2, 3, 4 |
| 🟡 中等 | 5 | 5, 6, 7, 8, 9 |
| 🟢 轻微 | 3 | 10, 11, 12 |
| **总计** | **12** | - |

---

## 🎯 修复优先级建议

### 立即修复 (P0)
1. **问题2**: SQL驱动单例并发问题 - 数据安全风险
2. **问题1**: 容器ID解析逻辑缺陷 - 功能错误风险

### 短期修复 (P1)
3. **问题3**: API层全局互斥锁性能瓶颈 - 性能优化
4. **问题4**: WebSocket心跳检测 - 稳定性提升
5. **问题9**: defer rows.Close() 修复 - 资源泄漏

### 中期改进 (P2)
6. **问题5**: 前端状态管理重构
7. **问题6**: API客户端封装
8. **问题8**: 单元测试覆盖

### 长期优化 (P3)
9. **问题7**: 配置验证增强
10. **问题10**: 代码重构减少重复
11. **问题11**: 类型定义整理
12. **问题12**: 日志规范制定

---

## 📝 备注

- 本文档应与 AGENTS.md 结合使用，作为代码审查的参考
- 建议将高优先级问题添加到项目的 Issue 跟踪系统
- 修复时应考虑向后兼容性，特别是 API 接口变更
- 建议在修复后为每个问题添加相应的测试用例
